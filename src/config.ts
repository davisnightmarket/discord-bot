import { WaitingForConfig } from './utility/config.utility';

// ok, this is now just a wrapper for GetConfig/WaitingForConfig from utility
// todo: delete it and just use the utility
export const Config = WaitingForConfig;
