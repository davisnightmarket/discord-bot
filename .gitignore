# Config.json includes secrets, so don't upload it.
*.secret.json
*.keys.json
config.json
keys.json
.env

# This stuff is automatically generated by nvm.
node_modules

# IDE nonsense
.vscode

# Build output
# we barely have enough memory to build crabapple on our cheap ass instance, so I'm building locally and keeping it in the rpo for now
# build
